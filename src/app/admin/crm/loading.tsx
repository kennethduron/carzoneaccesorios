import { AdminRouteLoading } from "@/components/route-loading-screens";

export default function Loading() {
  return <AdminRouteLoading title="Preparando CRM, prospectos y tareas" variant="crm" />;
}
