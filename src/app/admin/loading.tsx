import { AdminRouteLoading } from "@/components/route-loading-screens";

export default function Loading() {
  return <AdminRouteLoading title="Preparando el panel administrativo" variant="dashboard" />;
}
